// Navigation prototypes have been removed from the codebase.
// This stub keeps any remaining imports from breaking during a transitional commit.
import React from 'react';

export default function NavigationPrototypes(): React.ReactElement {
  return (
    <div style={{ padding: 24 }}>
      <h2>Navigation prototypes removed</h2>
      <p>The navigation prototypes demo was removed. If you reached this page, the prototype content has been cleaned up.</p>
    </div>
  );
}